## 1. Phase 01 - Конфиг, scaffold-policy, валидация ([plan](./artifacts/plan/phase-01.md))
- [x] 1.1 Phase: 01; Scope: отделить `scaffold_dir` от `generated_dir` и зафиксировать path-boundary правила внутри `project_dir`; Output artifact: `src/apidev/core/models/config.py`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/unit -k "scaffold and config"`, `uv run ruff check src tests`, `uv run mypy src`, security-check path traversal fixtures; DoD: scaffold output работает в независимом каталоге без ослабления boundary-гарантий.
- [x] 1.2 Phase: 01; Scope: добавить `generator.scaffold_write_policy` с режимами `create-missing`, `skip-existing`, `fail-on-conflict`; Output artifact: `src/apidev/core/models/config.py`, `src/apidev/application/services/diff_service.py`, `docs/reference/contract-format.md`; Verification: `uv run pytest tests/unit tests/integration -k "scaffold and policy"`, `uv run ruff check src tests`, config-contract checks, security-check conflict handling; DoD: все режимы политики детерминированы, валидируются и документированы.
- [x] 1.3 Phase: 01; Scope: поддержать short-form `errors[].example` и нормализацию в каноническую модель error example; Output artifact: `src/apidev/core/rules/contract_schema.py`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/unit -k "error and example"`, `apidev validate` fixtures для short/nested forms, `uv run ruff check src tests`; DoD: оба синтаксиса примеров поддерживаются и дают стабильную OpenAPI projection семантику.
- [x] 1.4 Phase: 01; Scope: усилить strict validation release-state (ошибка на `current_release` и неверный тип `release_number`) без миграционного fallback; Output artifact: `src/apidev/infrastructure/config/toml_loader.py`, `src/apidev/core/models/release_state.py`; Verification: `uv run pytest tests/unit -k "release_state and validation"`, negative fixtures для legacy key/type mismatch, `uv run ruff check src tests`; DoD: невалидный release-state завершается явной ошибкой валидации с понятным diagnostics payload.

## 2. Phase 02 - Runtime adapter и OpenAPI extensions ([plan](./artifacts/plan/phase-02.md))
- [x] 2.1 Phase: 02; Scope: реализовать runtime adapter для регистрации маршрутов через `APIRouter.add_api_route(...)` из operation metadata; Output artifact: `src/apidev/templates/integration_router_factory.py.j2`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/integration -k "router"`, generated-router snapshots, `uv run ruff check src tests`, metadata input safety checks; DoD: маршруты строятся автоматически из operation_map и проходят интеграционные проверки.
- [x] 2.2 Phase: 02; Scope: пробросить `operation_id`, summary/description/deprecated, auth и error/success responses в runtime/OpenAPI projection, а Swagger `tags` вычислять автоматически из домена операции; Output artifact: `src/apidev/templates/generated_operation_map.py.j2`, `src/apidev/templates/integration_router_factory.py.j2`, `src/apidev/templates/generated_openapi_docs.py.j2`; Verification: `uv run pytest tests/unit tests/integration -k "openapi or metadata or tags"`, snapshot consistency checks, negative fixtures на наличие manual `tags`, `uv run ruff check src tests`; DoD: runtime routing и OpenAPI metadata консистентны и детерминированы, доменная группировка Swagger не расходится со структурой операций.
- [ ] 2.3 Phase: 02; Scope: добавить `openapi.include_extensions` toggle для `x-apidev-*` extension fields; Output artifact: `src/apidev/core/models/config.py`, `src/apidev/templates/generated_openapi_docs.py.j2`, `docs/reference/cli-contract.md`; Verification: `uv run pytest tests/integration -k "extensions"`, OpenAPI snapshots on/off, `uv run ruff check src tests`, extension leakage security checks; DoD: toggle управляет только extensions и сохраняет прежнее default behavior.
- [ ] 2.4 Phase: 02; Scope: зафиксировать и проверить соответствие runtime adapter референсному integration-коду; Output artifact: `openspec/changes/012-integration/artifacts/design/05-integration-reference.md`, `tests/integration/**`; Verification: `uv run pytest tests/integration -k "router and operation_map and auto_register"`, router/openapi snapshots vs reference contract, `uv run ruff check src tests`; DoD: runtime wiring соответствует reference-контракту, новые endpoint-ы подключаются без ручного редактирования router definition.

## 3. Phase 03 - Integration profiles для init ([plan](./artifacts/plan/phase-03.md))
- [ ] 3.1 Phase: 03; Scope: добавить profile-флаги `--runtime`, `--integration-mode`, `--integration-dir` в `apidev init` и зафиксировать enum-контракт (`--runtime`: `fastapi|none`, `--integration-mode`: `off|scaffold|full`) + канонический CLI-синтаксис `--flag value`; Output artifact: `src/apidev/commands/init_cmd.py`, `src/apidev/cli.py`, `docs/reference/cli-contract.md`; Verification: `uv run pytest tests/unit -k "init_cmd"`, CLI contract checks, `uv run ruff check src tests`, integration-dir boundary security checks; DoD: CLI принимает profile-флаги, валидирует их и публикует однозначный help-контракт.
- [ ] 3.2 Phase: 03; Scope: синхронизировать lifecycle integration templates в режимах create/repair/force; Output artifact: `src/apidev/application/services/init_service.py`, `src/apidev/templates/*`; Verification: `uv run pytest tests/integration -k "init and integration"`, template matrix checks, `uv run ruff check src tests`, overwrite safety checks; DoD: `init` создает/обновляет integration artifacts предсказуемо и идемпотентно.
- [ ] 3.3 Phase: 03; Scope: зафиксировать UX для profile-based bootstrap без миграционного контура; Output artifact: `docs/reference/cli-contract.md`, `docs/reference/contract-format.md`; Verification: docs consistency checks, command example smoke validation, `uv run ruff check src tests`; DoD: документация покрывает profile UX, enum-значения и repair/force precedence.

## 4. Phase 04 - Стабилизация и readiness ([plan](./artifacts/plan/phase-04.md))
- [ ] 4.1 Phase: 04; Scope: обновить delta-спеку и связанный docs-контракт по всем добавленным возможностям; Output artifact: `openspec/changes/012-integration/specs/cli-tool-architecture/spec.md`, `docs/reference/contract-format.md`, `docs/reference/cli-contract.md`; Verification: `openspec validate 012-integration --strict --no-interactive`, docs-link checks, consistency review; DoD: спеки и документация синхронизированы с implement-plan без противоречий, а `spec + behavior + cli docs` содержат согласованную matrix для profile precedence и profile-managed artifacts.
- [ ] 4.2 Phase: 04; Scope: собрать regression matrix по scaffold/runtime/init и закрепить test coverage; Output artifact: `tests/unit/**`, `tests/integration/**`, `tests/contract/**`; Verification: `uv run pytest tests/unit tests/integration`, contract snapshot updates, `uv run ruff check src tests`, security regression checklist; DoD: regression suite закрывает ключевые сценарии и проходит стабильно.
- [ ] 4.3 Phase: 04; Scope: подготовить финальный handoff для отдельной implement-команды; Output artifact: `artifacts/plan/implementation-handoff.md`, `artifacts/plan/README.md`, `tasks.md`; Verification: cross-link integrity between plan/design/tasks, planning-only status audit, readiness review; DoD: change-пакет полностью готов к запуску `/openspec-implement`.
- [ ] 4.4 Phase: 04; Scope: синхронизировать repository-wide архитектурные документы с новым write-boundary правилом (`generated_dir`/`scaffold_dir` в пределах `project_dir`); Output artifact: `docs/architecture/architecture-overview.md`, `docs/architecture/architecture-rules.md`, `docs/architecture/validation-blueprint.md`, `docs/architecture/generated-integration.md`; Verification: архитектурный consistency review + `openspec validate 012-integration --strict --no-interactive`; DoD: boundary policy в change-пакете и `docs/architecture/*` совпадает без противоречий.
- [ ] 4.5 Phase: 04; Scope: [Non-blocking cleanup] провести non-functional рефакторинг внутреннего нейминга `generated_root` -> `generated_dir_path` (или эквивалент) в `src/**` и `tests/**` без изменения внешнего контракта `generator.generated_dir`; Output artifact: `src/apidev/**`, `tests/**`; Verification: `uv run pytest tests/unit tests/integration tests/contract`, `uv run ruff check src tests`, `uv run mypy src`; DoD: внутренний код и тесты используют единый термин, поведение CLI/контрактов и форматы конфигурации не изменены; пункт явно вне функционального acceptance scope фаз 01-03 и не блокирует approval/implement по функциональным задачам.

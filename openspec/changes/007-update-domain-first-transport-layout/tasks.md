## 0. Cross-change Dependency Gate
- [x] 0.1 Scope: зафиксировать dependency-order и совместимость с `002-transport-generation`, `006-safety-drift`, `005-contract-evolution-integration`; Output: `proposal.md`, `design.md`; Verification: `openspec validate 007-update-domain-first-transport-layout --strict --no-interactive`; DoD: в change-пакете явно описаны точки замещения/дополнения соседних change-контрактов.

## 1. Phase 01 - Domain-first Planning Contract ([plan](./artifacts/plan/phase-01.md))
- [x] 1.1 Scope: определить canonical mapping из contract relpath в domain-first generated paths для `<domain>/routes` и `<domain>/models`; Output: `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/unit/test_diff_service_transport_generation.py`; DoD: план изменений стабильно формирует только target paths вида `<generated_dir>/<domain>/{routes,models}/*`.
- [x] 1.2 Scope: обновить operation registry metadata на domain-qualified module paths (`<domain>.routes.*`, `<domain>.models.*`) и добавить `auth` signal; Output: `src/apidev/application/services/diff_service.py`, `src/apidev/templates/generated_operation_map.py.j2`; Verification: `uv run pytest tests/contract/architecture/test_transport_generation_contract.py`; DoD: registry ссылки и auth metadata соответствуют новой структуре и детерминированы.
- [x] 1.3 Scope: реализовать deterministic normalizer для single-level domain/module segments, reject nested contract paths и fail-fast collision diagnostics; Output: `src/apidev/application/services/diff_service.py`, `tests/unit/test_diff_service_transport_generation.py`; Verification: `uv run pytest tests/unit/test_diff_service_transport_generation.py -k \"single-level or nested or collision or normalize\"`; DoD: `contract_relpath -> module path` однозначен, nested layout отвергается стабильно, коллизии диагностируются детерминированно.
- [x] 1.4 Scope: зафиксировать package/import policy для single-level domain layout (`<domain>/routes`, `<domain>/models`) и генерацию `__init__.py`; Output: `src/apidev/templates/generated_router.py.j2`, `src/apidev/templates/generated_schema.py.j2`, `src/apidev/templates/generated_operation_map.py.j2`, `docs/architecture/generated-integration.md`; Verification: `uv run pytest tests/contract tests/integration`; DoD: runtime wiring imports разрешаются без ручных path hacks и package-файлы создаются детерминированно.
- [x] 1.5 Scope: обновить contract/integration assertions на новый canonical layout; Output: `tests/contract/**`, `tests/integration/**`; Verification: `uv run pytest tests/contract tests/integration`; DoD: тесты больше не зависят от operation-first путей.

## 2. Phase 02 - Template & Runtime Wiring Update ([plan](./artifacts/plan/phase-02.md))
- [x] 2.1 Scope: обновить шаблоны router/schema imports под структуру `<domain>/routes` и `<domain>/models`; Output: `src/apidev/templates/generated_router.py.j2`, `src/apidev/templates/generated_schema.py.j2`; Verification: `uv run pytest tests/integration/test_generate_roundtrip.py -k generate`; DoD: generated код компилируется и использует корректные domain-qualified imports.
- [x] 2.2 Scope: зафиксировать deterministic generation output в новом layout; Output: `src/apidev/application/services/diff_service.py`, `tests/unit/test_diff_service_transport_generation.py`; Verification: `uv run pytest tests/unit/test_diff_service_transport_generation.py`; DoD: повторная генерация на неизменных контрактах byte-stable.
- [x] 2.3 Scope: синхронизировать runtime wiring через operation map с новым layout; Output: `src/apidev/templates/generated_operation_map.py.j2`, `tests/contract/architecture/test_transport_generation_contract.py`; Verification: `uv run pytest tests/contract/architecture/test_transport_generation_contract.py`; DoD: registry/bridge references соответствуют путям `<domain>.routes.*` и `<domain>.models.*`.
- [x] 2.7 Scope: зафиксировать контракт передачи authenticated user context в manual handler через integration layer; Output: integration docs/contracts + runtime wiring tests; Verification: `uv run pytest tests/integration -k \"auth and integration\"`; DoD: user context передается в handler input, token decode остается manual.
- [x] 2.4 Scope: реализовать контракт scaffold-конфигурации и CLI overrides (`generator.scaffold`, `generator.scaffold_dir`, `--scaffold`, `--no-scaffold`) с политикой `create-if-missing`; Output: `src/apidev/core/models/config.py`, `src/apidev/commands/generate_cmd.py`, `src/apidev/commands/diff_cmd.py`, `src/apidev/application/services/generate_service.py`, templates integration scaffold; Verification: `uv run pytest tests/unit tests/integration -k scaffold`; DoD: `apidev init` создает дефолты scaffold-конфига, CLI overrides работают, существующие scaffold-файлы не перезаписываются.
- [x] 2.5 Scope: исключить scaffold-файлы из stale-remove при включенном scaffold; Output: `src/apidev/application/services/diff_service.py`, `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration -k \"scaffold and remove\"`; DoD: существующие scaffold-файлы не помечаются как `REMOVE`.
- [x] 2.8 Scope: зафиксировать remove-semantics для `--no-scaffold`/`generator.scaffold=false`; Output: `src/apidev/application/services/diff_service.py`, `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration -k \"no-scaffold and remove\"`; DoD: scaffold-файлы при disabled режиме обрабатываются как stale в рамках safety-boundary.
- [x] 2.6 Scope: использовать контрактный `response.status` в generated OpenAPI responses; Output: `src/apidev/templates/generated_openapi_docs.py.j2`, related metadata build; Verification: `uv run pytest tests/integration/test_generate_roundtrip.py -k openapi`; DoD: OpenAPI status соответствует contract `response.status`, без hardcoded `200`.

## 3. Phase 03 - Docs & Verification Sync ([plan](./artifacts/plan/phase-03.md))
- [x] 3.1 Scope: обновить reference-документацию expected output layout; Output: `docs/reference/contract-format.md`, `docs/reference/cli-contract.md`; Verification: `make docs-check`; DoD: документация явно описывает domain-first generated layout как canonical.
- [x] 3.2 Scope: зафиксировать подробный integration contract (registry/factory, endpoint/auth/error/openapi) с примерами и диаграммами; Output: `docs/architecture/generated-integration.md`; Verification: `make docs-check`; DoD: интеграционный workflow полностью описан и согласован с ownership boundary.
- [x] 3.3 Scope: обновить testing strategy regression-кейсами для нового layout; Output: `docs/process/testing-strategy.md`; Verification: `make docs-check`; DoD: checklist покрывает validate/diff/gen/gen-check для domain-first структуры.
- [x] 3.5 Scope: синхронизировать migration notes по переходу operation-first -> domain-first в reference docs; Output: `docs/reference/contract-format.md`, `docs/reference/cli-contract.md`; Verification: `make docs-check`; DoD: migration impact и отсутствие backward compatibility явно описаны для implement-phase.
- [x] 3.4 Scope: выполнить strict валидацию change-пакета; Output: `openspec validate 007-update-domain-first-transport-layout --strict --no-interactive`; Verification: strict validation pass; DoD: change готов к implement-фазе.

## 1. Phase 01 - REMOVE Planning Contract ([plan](./artifacts/plan/phase-01.md))
- [ ] 1.1 Scope: добавить `REMOVE` в generation plan и правила выделения stale generated artifacts; Output: `src/apidev/application/dto/generation_plan.py`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/unit -k "diff_service and remove"`; DoD: plan содержит детерминированные `REMOVE` для stale artifacts внутри generated root.
- [ ] 1.2 Scope: синхронизировать drift-предикат `ADD|UPDATE|REMOVE` для `apidev diff` и `apidev gen --check`; Output: `src/apidev/commands/diff_cmd.py`, `src/apidev/application/services/generate_service.py`; Verification: `uv run pytest tests/integration -k "drift and remove"`; DoD: remove-only кейс возвращает `drift` и ожидаемые exit semantics в обоих read-only режимах.
- [ ] 1.3 Scope: зафиксировать deterministic ordering для remove-операций; Output: `src/apidev/application/services/diff_service.py`, `tests/unit/test_diff_service_transport_generation.py`; Verification: `uv run pytest tests/unit -k "deterministic and remove"`; DoD: повторные запуски на неизменном входе дают эквивалентный remove-план.

## 2. Phase 02 - REMOVE Apply Safety ([plan](./artifacts/plan/phase-02.md))
- [ ] 2.1 Scope: расширить apply-контур `apidev gen` на `REMOVE`; Output: `src/apidev/application/services/generate_service.py`; Verification: `uv run pytest tests/integration -k "generate and remove"`; DoD: apply-режим удаляет stale generated artifacts и возвращает `no-drift` при успехе.
- [ ] 2.2 Scope: обеспечить boundary enforcement для remove-операций; Output: `src/apidev/infrastructure/output/writer.py`, `tests/contract/architecture/test_write_boundary_policy.py`; Verification: `uv run pytest tests/contract/architecture/test_write_boundary_policy.py`; DoD: remove вне generated root отклоняется с явной safety ошибкой.
- [ ] 2.3 Scope: добавить канонические diagnostic codes `remove-conflict` и `remove-boundary-violation` и минимальную схему diagnostics (`code`, `location`, `detail`); Output: `src/apidev/application/services/generate_service.py`, `src/apidev/application/services/diff_service.py`, `tests/unit/**`; Verification: `uv run pytest tests/unit -k "remove and diagnostic"`; DoD: конфликтные remove-сценарии возвращают строго один из фиксированных кодов, machine-readable diagnostics по фиксированной схеме и `error` status.

## 3. Phase 03 - Verification & Docs Sync ([plan](./artifacts/plan/phase-03.md))
- [ ] 3.1 Scope: синхронизировать CLI контракт для remove-case drift/exit semantics; Output: `docs/reference/cli-contract.md`; Verification: `uv run pytest tests/unit/test_cli_conventions.py`; DoD: документация явно описывает remove-only кейсы без противоречий.
- [ ] 3.2 Scope: обновить testing strategy checklist remove-сценариями; Output: `docs/process/testing-strategy.md`; Verification: `make docs-check`; DoD: verification checklist включает remove-only и remove-conflict тесты.
- [ ] 3.3 Scope: добавить интеграционный regression для полного remove-roundtrip; Output: `tests/integration/test_generate_roundtrip.py`; Verification: `uv run pytest tests/integration/test_generate_roundtrip.py`; DoD: сценарий generate -> remove contract -> diff/check/gen -> no-drift воспроизводимо зеленый.
- [ ] 3.4 Scope: выполнить финальную валидацию change-пакета; Output: `openspec validate 006-safety-drift --strict --no-interactive`; Verification: strict validation pass; DoD: change готов к отдельной implement-команде.

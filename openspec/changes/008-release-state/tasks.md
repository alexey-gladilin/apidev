## 1. Phase 01 - Release-State Lifecycle Core ([plan](./artifacts/plan/phase-01.md))
- [x] 1.1 Scope: добавить apply-only hook для auto-create release-state в `apidev gen`; Output: `src/apidev/application/services/generate_service.py`; Verification: `uv run pytest tests/unit -k "release_state and create"`; DoD: при отсутствии файла `gen apply` создает валидный release-state с обязательными полями только если baseline_ref успешно резолвится по policy precedence.
- [x] 1.2 Scope: реализовать bump policy `release_number` на основе фактически примененных изменений (`ADD|UPDATE|REMOVE`); Output: `src/apidev/application/services/generate_service.py`, `tests/unit/**`; Verification: `uv run pytest tests/unit -k "release_number and bump"`; DoD: `release_number` увеличивается только при applied changes и не меняется на no-op.
- [x] 1.3 Scope: зафиксировать baseline sync policy для apply-mode (`CLI -> release-state -> git fallback`) в release-state payload; Output: `src/apidev/application/services/generate_service.py`, `tests/unit/**`; Verification: `uv run pytest tests/unit -k "baseline_ref and precedence"`; DoD: итоговый `baseline_ref` в release-state соответствует contract precedence.
- [x] 1.4 Scope: зафиксировать fail-without-write поведение для `no-git/no-baseline` в apply-mode; Output: `src/apidev/application/services/generate_service.py`, `tests/unit/**`; Verification: `uv run pytest tests/unit -k "baseline_missing and release_state"`; DoD: при нерезолвимом baseline `gen apply` возвращает `baseline-missing|baseline-invalid` и не создает/не изменяет release-state.

## 2. Phase 02 - CLI/Diagnostics Integration ([plan](./artifacts/plan/phase-02.md))
- [x] 2.1 Scope: обеспечить отсутствие release-state write side effects в `diff` и `gen --check`; Output: `src/apidev/application/services/diff_service.py`, `src/apidev/commands/generate_cmd.py`, `tests/integration/**`; Verification: `uv run pytest tests/integration/test_compatibility_policy_cli.py -k "diff or check"`; DoD: read-only режимы не меняют release-state content/mtime.
- [x] 2.2 Scope: синхронизировать диагностику no-git/no-baseline сценариев с existing compatibility taxonomy; Output: `src/apidev/application/services/diff_service.py`, `tests/integration/**`; Verification: `uv run pytest tests/integration/test_compatibility_policy_cli.py -k "baseline"`; DoD: сценарии отражаются через стабильные diagnostics `baseline-missing|baseline-invalid|release-state-invalid`.
- [x] 2.3 Scope: гарантировать write-after-success для release-state sync (без частичных мутаций при apply error); Output: `src/apidev/application/services/generate_service.py`, `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration -k "release_state and error"`; DoD: при ошибке apply release-state остается в pre-run состоянии.

## 3. Phase 03 - Docs and Validation Sync ([plan](./artifacts/plan/phase-03.md))
- [x] 3.1 Scope: обновить reference docs по lifecycle release-state в `gen apply`; Output: `docs/reference/contract-format.md`, `docs/reference/cli-contract.md`; Verification: `make docs-check`; DoD: документация явно фиксирует auto-create и bump semantics.
- [x] 3.2 Scope: добавить/обновить unit+integration регрессии для auto-create/bump/no-op и read-only инвариантов; Output: `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration`; DoD: regression coverage закрывает новые lifecycle-сценарии.
- [x] 3.3 Scope: выполнить strict валидацию change-пакета и подтвердить implement readiness; Output: `openspec validate 008-release-state --strict --no-interactive`; Verification: strict validation pass; DoD: пакет готов к отдельной implement-фазе.

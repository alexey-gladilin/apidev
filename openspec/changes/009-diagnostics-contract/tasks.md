## 1. Phase 01 - Unified Diagnostics Model and CLI Surface ([plan](./artifacts/plan/phase-01.md))
- [x] 1.1 Scope: спроектировать и внедрить единый diagnostics envelope/serializer для `validate|diff|gen`; Output: `src/apidev/application/dto/diagnostics.py`, `src/apidev/application/dto/generation_plan.py`, `src/apidev/core/models/diagnostic.py`; Verification: `uv run pytest tests/unit -k "diagnostic and json"`; DoD: все команды используют единый machine-readable schema с обязательными полями `code|severity|location|message`.
- [x] 1.2 Scope: добавить/нормализовать machine-readable режим для `apidev diff` и `apidev gen` с паритетом к `validate`; Output: `src/apidev/commands/diff_cmd.py`, `src/apidev/commands/generate_cmd.py`, `src/apidev/commands/validate_cmd.py`; Verification: `uv run pytest tests/unit/test_cli_conventions.py`; DoD: `validate/diff/gen` поддерживают единый JSON contract и предсказуемые exit semantics.
- [x] 1.3 Scope: унифицировать taxonomy diagnostics codes по namespace `validation|compatibility|generation|runtime|config`; Output: `src/apidev/application/services/diff_service.py`, `src/apidev/application/services/generate_service.py`, `src/apidev/application/services/validate_service.py`; Verification: `uv run pytest tests/unit -k "diagnostic code"`; DoD: диагностические коды документированы и стабильны между повторными запусками.

## 2. Phase 02 - Compatibility and Drift Reporting Alignment ([plan](./artifacts/plan/phase-02.md))
- [x] 2.1 Scope: выровнять compatibility diagnostics format в `diff` и `gen --check` под общий envelope без потери текущей policy-семантики; Output: `src/apidev/commands/common/compatibility.py`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/integration/test_compatibility_policy_cli.py`; DoD: `warn|strict` поведение и `policy_blocked` сохраняются, JSON/summary консистентны.
- [x] 2.2 Scope: закрепить детерминированность порядка diagnostics и summary counters для CI parsing; Output: `src/apidev/application/services/validate_service.py`, `src/apidev/application/services/diff_service.py`, `src/apidev/application/services/generate_service.py`; Verification: `uv run pytest tests/unit -k "deterministic and diagnostics"`; DoD: идентичный вход всегда дает идентичный порядок diagnostics и summary.
- [x] 2.3 Scope: обеспечить обратную совместимость plain-text UX при добавлении JSON контракта; Output: `src/apidev/commands/diff_cmd.py`, `src/apidev/commands/generate_cmd.py`; Verification: `uv run pytest tests/unit/test_cli_conventions.py tests/integration/test_generate_roundtrip.py`; DoD: text output остается читаемым и не ломает текущие пользовательские сценарии.

## 3. Phase 03 - Docs, Tests, and Contract Validation Sync ([plan](./artifacts/plan/phase-03.md))
- [x] 3.1 Scope: обновить reference docs diagnostics contract и JSON schema для всех CLI режимов; Output: `docs/reference/cli-contract.md`, `docs/process/testing-strategy.md`; Verification: `make docs-check`; DoD: документация синхронизирована с фактическим CLI поведением.
- [x] 3.2 Scope: добавить regression tests на unified diagnostics envelope, codes taxonomy и exit semantics; Output: `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration`; DoD: тесты покрывают validate/diff/gen(+--check) для success/drift/error сценариев.
- [x] 3.3 Scope: выполнить strict validation change-пакета и подтвердить implement readiness; Output: `openspec validate 009-diagnostics-contract --strict --no-interactive`; Verification: strict validation pass; DoD: пакет готов к отдельной implement-фазе.

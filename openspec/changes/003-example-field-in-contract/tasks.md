## 1. Phase 01 - Schema Contract Extension ([plan](./artifacts/plan/phase-01.md))
- [ ] 1.1 Scope: расширить contract schema allowlist для `example` во всех schema-фрагментах; Output: `src/apidev/core/rules/contract_schema.py`; Verification: `uv run pytest tests/unit/test_validate_service.py -k example`; DoD: `example` не считается unknown field в разрешенных узлах.
- [ ] 1.2 Scope: зафиксировать запрет root-level блока `examples` как unknown-field regression guard; Output: `src/apidev/core/rules/contract_schema.py`; Verification: `uv run pytest tests/unit/test_validate_service.py -k "examples and root"`; DoD: `examples` на root уровне отклоняется детерминированной диагностикой.
- [ ] 1.3 Scope: реализовать строгую валидацию совместимости schema-level `example` с `type/enum` и container shape; Output: `src/apidev/core/rules/contract_schema.py`; Verification: `uv run pytest tests/unit/test_validate_service.py -k "example and invalid"`; DoD: диагностируются типовые несоответствия и enum-мисматчи.
- [ ] 1.4 Scope: обновить domain model при необходимости для явного хранения schema-level `example`; Output: `src/apidev/core/models/contract.py`; Verification: `uv run pytest tests/unit/test_validate_service.py`; DoD: модель не теряет примеры при pipeline-прохождении.

## 2. Phase 02 - Generation and OpenAPI Propagation ([plan](./artifacts/plan/phase-02.md))
- [ ] 2.1 Scope: пробросить `example` в generation metadata и contract fingerprint payload; Output: `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/unit/test_diff_service_transport_generation.py`; DoD: изменения в `example` детектируются как drift.
- [ ] 2.2 Scope: расширить generated schema template для экспонирования `example` в transport-моделях; Output: `src/apidev/templates/generated_schema.py.j2`; Verification: `uv run pytest tests/integration/test_generate_roundtrip.py -k example`; DoD: generated schema содержит deterministic representation примеров.
- [ ] 2.3 Scope: расширить OpenAPI template для включения schema-level `example` в response/error metadata; Output: `src/apidev/templates/generated_openapi_docs.py.j2`; Verification: `uv run pytest tests/integration/test_generate_roundtrip.py -k openapi`; DoD: OpenAPI fragment отражает schema-level примеры без недетерминизма.

## 3. Phase 03 - Documentation and Tests ([plan](./artifacts/plan/phase-03.md))
- [ ] 3.1 Scope: добавить unit positive/negative кейсы для schema-level `example`; Output: `tests/unit/test_validate_service.py`; Verification: `uv run pytest tests/unit/test_validate_service.py`; DoD: покрыты success/type-mismatch/enum-mismatch/shape-mismatch и root-level `examples` unknown-field сценарии.
- [ ] 3.2 Scope: добавить generation regression tests на стабильность output с `example`; Output: `tests/unit/test_diff_service_transport_generation.py`, `tests/integration/test_generate_roundtrip.py`; Verification: `uv run pytest tests/unit/test_diff_service_transport_generation.py tests/integration/test_generate_roundtrip.py`; DoD: повторная генерация byte-stable при неизменных контрактах.
- [ ] 3.3 Scope: обновить reference contract format documentation; Output: `docs/reference/contract-format.md`; Verification: `uv run pytest tests/unit/test_documentation_conventions.py`; DoD: документация описывает синтаксис, ограничения и примеры использования `example`.

## 4. Phase 04 - Change Quality Gate ([plan](./artifacts/plan/README.md))
- [ ] 4.1 Scope: синхронизировать links между proposal/design/tasks/specs/plan artifacts; Output: [artifacts/plan/README.md](./artifacts/plan/README.md); Verification: `rg -n "artifacts/|specs/contract-examples" openspec/changes/003-example-field-in-contract`; DoD: все core-файлы имеют traceability links.
- [ ] 4.2 Scope: выполнить strict OpenSpec validation; Output: `openspec validate 003-example-field-in-contract --strict --no-interactive`; Verification: strict validation pass; DoD: change готова к отдельной implement-команде.

## 1. Phase 01 - Контрактная схема и модель request ([plan](./artifacts/plan/phase-01.md))
- [ ] 1.1 Phase: 01; Scope: добавить root-блок `request` и поддержать только `request.path|request.query|request.body` с fail-fast запретом unknown fields; Output artifact: `src/apidev/core/rules/contract_schema.py`; Verification: `uv run pytest tests/unit -k "contract_schema and request and unknown"`, `uv run pytest tests/integration -k "validate and request"`; DoD: `apidev validate` принимает валидный `request` и стабильно отклоняет неразрешенные поля с точным diagnostics path.
- [ ] 1.2 Phase: 01; Scope: реализовать строгую проверку согласованности `request.path` с `{param}` в route path (missing/extra mismatch -> fail-fast); Output artifact: `src/apidev/core/rules/contract_schema.py`, `tests/unit/**`; Verification: `uv run pytest tests/unit -k "path and request and mismatch"`, `uv run pytest tests/integration -k "validate and path"`; DoD: несовпадение наборов path-параметров всегда завершает валидацию предсказуемой ошибкой.
- [ ] 1.3 Phase: 01; Scope: расширить `EndpointContract` request-полями и синхронизировать маппинг валидированного контракта в модель; Output artifact: `src/apidev/core/models/contract.py`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/unit -k "endpoint_contract and request_model"`; DoD: контрактная модель содержит нормализованные request fragments (`path/query/body`) и доступна downstream-генераторам без эвристик.

## 2. Phase 02 - Генерация request metadata и OpenAPI ([plan](./artifacts/plan/phase-02.md))
- [ ] 2.1 Phase: 02; Scope: добавить публикацию request metadata (`path/query/body`) в generated `operation_map`; Output artifact: `src/apidev/templates/generated_operation_map.py.j2`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/unit -k "operation_map and request"`, `uv run pytest tests/integration -k "gen and operation_map"`; DoD: каждая операция в `operation_map` содержит согласованный request metadata contract.
- [ ] 2.2 Phase: 02; Scope: генерировать request transport model из контрактного request schema fragment вместо пустого `{}`; Output artifact: `src/apidev/templates/generated_schema.py.j2`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/integration -k "request_model and gen"`, snapshot checks generated request schema; DoD: generated request-модель отражает `request.path/query/body` и детерминирована между повторными запусками.
- [ ] 2.3 Phase: 02; Scope: расширить OpenAPI projection для `parameters` (`in: path|query`) и `requestBody` на базе request metadata с корректной опциональностью query/body; Output artifact: `src/apidev/templates/generated_openapi_docs.py.j2`, `src/apidev/application/services/diff_service.py`; Verification: `uv run pytest tests/integration -k "openapi and request and parameters and requestBody"`, OpenAPI snapshot checks; DoD: OpenAPI проекция полностью и только из контрактной request-модели, без лишних параметров для операций без `query/body`.

## 3. Phase 03 - Тесты, документация и readiness ([plan](./artifacts/plan/phase-03.md))
- [ ] 3.1 Phase: 03; Scope: добавить regression-покрытие unit/integration/contract для request validation, operation_map, request-model и OpenAPI projection; Output artifact: `tests/unit/**`, `tests/integration/**`, `tests/contract/**`; Verification: `uv run pytest tests/unit tests/integration tests/contract -k "request or operation_map or openapi"`; DoD: тесты покрывают happy/fail-fast сценарии и защищают от дрейфа request-контракта.
- [ ] 3.2 Phase: 03; Scope: обновить reference-документацию формата контракта и CLI-контракта под `request.path|query|body`; Output artifact: `docs/reference/contract-format.md`, `docs/reference/cli-contract.md`; Verification: docs consistency review + примеры `apidev validate/gen` по новым request-сценариям; DoD: документация описывает точный контракт request-полей, ошибки валидации и ожидаемую OpenAPI проекцию.
- [ ] 3.3 Phase: 03; Scope: выполнить финальный readiness-gate change-пакета (plan/design/tasks/spec sync + strict OpenSpec validation); Output artifact: `openspec/changes/013-extend-contract/artifacts/plan/*`, `openspec/changes/013-extend-contract/tasks.md`; Verification: `openspec validate 013-extend-contract --strict --no-interactive`; DoD: change-пакет проходит strict validation и готов к запуску `/openspec-implement`.

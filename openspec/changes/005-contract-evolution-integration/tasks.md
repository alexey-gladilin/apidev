## 1. Phase 01 - Compatibility Classification Contract ([plan](./artifacts/plan/phase-01.md))
- [x] 1.1 Scope: формализовать taxonomy изменений (`non-breaking`, `potentially-breaking`, `breaking`) и правила классификации для contract diff; Output: [artifacts/design/02-behavior.md](./artifacts/design/02-behavior.md); Verification: traceability review against [specs/contract-evolution-integration/spec.md](./specs/contract-evolution-integration/spec.md); DoD: определены категории, критерии и ожидаемый CLI результат по каждой категории.
- [x] 1.2 Scope: определить mapping compatibility classification в diagnostics/exit behavior для `apidev diff` и `apidev gen --check`; Output: [artifacts/design/03-decisions.md](./artifacts/design/03-decisions.md); Verification: consistency check с `docs/reference/cli-contract.md`; DoD: зафиксированы backward-compatible defaults и fail-fast режимы.
- [x] 1.3 Scope: зафиксировать минимальные extension points в `core/rules/compatibility.py` и приложении сервисов; Output: [artifacts/plan/phase-01.md](./artifacts/plan/phase-01.md); Verification: architecture review с [artifacts/design/01-architecture.md](./artifacts/design/01-architecture.md); DoD: определены интерфейсы без смешения core/application/infra слоев.

## 2. Phase 02 - Optional `dbspec` Integration Contract ([plan](./artifacts/plan/phase-02.md))
- [x] 2.1 Scope: определить read-only contract загрузки schema hints из `dbspec` (types/nullability/refs/enums) и стратегию graceful fallback; Output: [artifacts/design/02-behavior.md](./artifacts/design/02-behavior.md); Verification: alignment check с `docs/product/vision.md`; DoD: отсутствие `dbspec` не блокирует основной workflow.
- [x] 2.2 Scope: определить deterministic merge policy между contract data и optional dbspec hints; Output: [artifacts/design/03-decisions.md](./artifacts/design/03-decisions.md); Verification: deterministic review against existing generation invariants; DoD: одинаковые входы дают byte-stable output.
- [x] 2.3 Scope: определить ограничения безопасности/границ ownership для интеграции (APIDev не владеет DB schema artifacts); Output: [artifacts/design/01-architecture.md](./artifacts/design/01-architecture.md); Verification: architecture-rules review with `docs/architecture/architecture-rules.md`; DoD: зафиксированы запреты на write/coupling и обязательный read-only режим.

## 3. Phase 03 - Formal Deprecation Policy ([plan](./artifacts/plan/phase-03.md))
- [x] 3.1 Scope: определить lifecycle deprecation (`active`, `deprecated`, `removed`) и обязательные metadata-поля/маркеры; Output: [artifacts/design/02-behavior.md](./artifacts/design/02-behavior.md); Verification: scenario review against [specs/contract-evolution-integration/spec.md](./specs/contract-evolution-integration/spec.md); DoD: описан детерминированный жизненный цикл с transition rules.
- [x] 3.2 Scope: определить policy для перехода к удалению (минимальный grace period, предупреждения, breaking classification); Output: [artifacts/design/03-decisions.md](./artifacts/design/03-decisions.md); Verification: consistency review with compatibility taxonomy; DoD: удаление без deprecation window классифицируется как breaking.
- [x] 3.3 Scope: зафиксировать стратегию документации deprecation в CLI/docs/generated metadata; Output: [artifacts/plan/phase-03.md](./artifacts/plan/phase-03.md); Verification: docs traceability review; DoD: определены места и формат отображения статуса deprecation.
- [x] 3.4 Scope: зафиксировать контракт persistence для release state (source-of-truth path, JSON-формат с `baseline_ref`, override через `.apidev/config.toml`, запрет write в read-only режимах); Output: [artifacts/design/02-behavior.md](./artifacts/design/02-behavior.md), [artifacts/design/03-decisions.md](./artifacts/design/03-decisions.md); Verification: consistency review with `docs/reference/cli-contract.md`; DoD: исключены скрытые зависимости и неявные side effects.
- [x] 3.5 Scope: зафиксировать baseline snapshot contract для compatibility classification (VCS/CI source of truth, формат, сравнение по `baseline_ref`, поведение при отсутствии/невалидности baseline); Output: [artifacts/design/02-behavior.md](./artifacts/design/02-behavior.md), [artifacts/design/03-decisions.md](./artifacts/design/03-decisions.md); Verification: traceability review against [specs/contract-evolution-integration/spec.md](./specs/contract-evolution-integration/spec.md); DoD: классификация breaking/non-breaking опирается на явный baseline, воспроизводимый на разных машинах.

## 4. Phase 04 - Verification & Handoff ([plan](./artifacts/plan/implementation-handoff.md))
- [x] 4.1 Scope: зафиксировать тестовую матрицу unit/contract/integration для classification/integration/deprecation; Output: [artifacts/design/04-testing.md](./artifacts/design/04-testing.md); Verification: planned commands review (`uv run pytest tests/unit tests/contract tests/integration`); DoD: описаны positive/negative/regression сценарии и quality gates.
- [x] 4.2 Scope: синхронизировать links между proposal/design/tasks/specs/artifacts и подготовить implement handoff; Output: [artifacts/plan/README.md](./artifacts/plan/README.md); Verification: `rg -n "artifacts/|specs/contract-evolution-integration" openspec/changes/005-contract-evolution-integration`; DoD: полная traceability между core файлами и артефактами.
- [x] 4.3 Scope: выполнить strict OpenSpec validation; Output: `openspec validate 005-contract-evolution-integration --strict --no-interactive`; Verification: validation pass; DoD: change готова к отдельной implement-команде.

## 5. Phase 05 - Implementation Plan (for `/openspec-implement`) ([plan](./artifacts/plan/phase-04-implementation.md))
- [x] 5.1 Scope: реализовать compatibility classifier и taxonomy в `src/apidev/core/rules/compatibility.py`; Output: `src/apidev/core/rules/compatibility.py`, `tests/unit/**`; Verification: `uv run pytest tests/unit`; DoD: классификация `non-breaking|potentially-breaking|breaking` детерминирована и покрыта unit-тестами.
- [x] 5.2 Scope: интегрировать compatibility diagnostics/exit behavior в `diff_service` и `generate_service` с backward-compatible default policy `warn`; Output: `src/apidev/application/services/diff_service.py`, `src/apidev/application/services/generate_service.py`, `tests/integration/**`; Verification: `uv run pytest tests/integration`; DoD: `diff` и `gen --check` возвращают ожидаемые diagnostics/exit semantics.
- [x] 5.3 Scope: добавить конфиг-контракт policy/release-state (`.apidev/config.toml`) и валидацию параметров (`evolution.grace_period_releases>=1`, `evolution.release_state_file`) плюс валидацию JSON-формата `.apidev/release-state.json` (включая обязательный `baseline_ref`); Output: `src/apidev/core/models/**`, `docs/reference/contract-format.md`, `tests/unit/**`; Verification: `uv run pytest tests/unit`; DoD: при невалидном конфиге или release-state возвращаются явные ошибки с диагностикой.
- [x] 5.4 Scope: реализовать read-only загрузку optional `dbspec` hints с deterministic merge policy (contract fields priority); Output: `src/apidev/application/services/**`, `tests/contract/**`, `tests/integration/**`; Verification: `uv run pytest tests/contract tests/integration`; DoD: отсутствие `dbspec` не блокирует workflow, merge-результат byte-stable.
- [x] 5.5 Scope: реализовать deprecation lifecycle checks и deprecation-window классификацию с release-state read-only semantics в check/diff режимах; Output: `src/apidev/application/services/**`, `src/apidev/core/models/**`, `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration`; DoD: удаление до окна классифицируется как breaking, read-only режимы не пишут release-state.
- [x] 5.6 Scope: реализовать baseline source resolution с явным приоритетом `--baseline-ref` (CLI override над release-state) и валидацией значения; Output: `src/apidev/commands/diff_cmd.py`, `src/apidev/commands/generate_cmd.py`, `src/apidev/application/services/**`, `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration`; DoD: источник baseline детерминирован, applied baseline отражается в diagnostics, некорректный override возвращает явную ошибку.
- [x] 5.7 Scope: реализовать baseline snapshot compare pipeline (`current_normalized_model` vs baseline из `baseline_ref`) и диагностику `baseline-missing|baseline-invalid`; Output: `src/apidev/application/services/**`, `src/apidev/core/models/**`, `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration`; DoD: breaking classification не выполняется без валидного baseline, strict policy корректно фейлит отсутствие/повреждение baseline, локальная потеря cache не ломает сравнение при доступном VCS.
- [x] 5.8 Scope: обновить CLI и документацию контрактов для новых policy/release-state/baseline/deprecation semantics; Output: `src/apidev/commands/diff_cmd.py`, `src/apidev/commands/generate_cmd.py`, `docs/reference/cli-contract.md`, `docs/reference/contract-format.md`; Verification: `uv run pytest tests/unit/test_cli_conventions.py`; DoD: справка и reference-документы синхронизированы, CLI-поведение задокументировано без противоречий.
- [x] 5.9 Scope: реализовать отображение deprecation status в generated metadata и шаблонах generated artifacts с regression coverage; Output: `src/apidev/templates/**`, `src/apidev/application/services/**`, `tests/contract/**`, `tests/integration/**`; Verification: `uv run pytest tests/contract tests/integration`; DoD: deprecation status последовательно отражается в diagnostics и generated metadata без нарушения deterministic output.

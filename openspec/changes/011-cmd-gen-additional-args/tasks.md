## 1. Phase 01 - CLI Surface и контракт фильтрации ([plan](./artifacts/plan/phase-01.md))
- [ ] 1.1 Phase: 01; Scope: добавить аргументы `--include-endpoint`/`--exclude-endpoint` в `apidev gen` с валидацией входа и нормализацией значений; Output artifact: `src/apidev/commands/generate_cmd.py`; Verification: `uv run pytest tests/unit/test_cli_conventions.py -k "include_endpoint or exclude_endpoint"`; DoD: CLI принимает множественные флаги, публикует корректный help и детерминированно отклоняет невалидный ввод.
- [ ] 1.2 Phase: 01; Scope: расширить application DTO/контракт вызова generate-пайплайна для передачи endpoint-фильтров; Output artifact: `src/apidev/application/**`; Verification: `uv run pytest tests/unit -k "generate and endpoint_filter"`; DoD: фильтры передаются из CLI в сервисный слой без скрытых глобальных зависимостей.

## 2. Phase 02 - Endpoint selection в generation pipeline ([plan](./artifacts/plan/phase-02.md))
- [ ] 2.1 Phase: 02; Scope: реализовать deterministic include/exclude selection операций перед построением generation plan; Output artifact: `src/apidev/application/services/diff_service.py`, `src/apidev/application/services/generate_service.py`; Verification: `uv run pytest tests/unit -k "diff_service and endpoint_filter"`; DoD: effective set формируется по правилу include->exclude и стабилен между повторными запусками.
- [ ] 2.2 Phase: 02; Scope: добавить diagnostics для empty effective set и invalid pattern с предсказуемыми code/location/detail; Output artifact: `src/apidev/application/dto/generation_plan.py`, `src/apidev/commands/generate_cmd.py`; Verification: `uv run pytest tests/integration -k "gen and endpoint_filter and diagnostics"`; DoD: пользователь получает проверяемый failure contract в text и JSON режимах.
- [ ] 2.3 Phase: 02; Scope: зафиксировать безопасное поведение stale-remove в частичной генерации (фильтрованный scope); Output artifact: `src/apidev/application/services/diff_service.py`, `src/apidev/application/services/generate_service.py`; Verification: `uv run pytest tests/integration -k "remove and endpoint_filter"`; DoD: удаление не выходит за policy boundary и не затрагивает endpoint-ы вне выбранного scope без явного контракта.

## 3. Phase 03 - Документация, regression и readiness ([plan](./artifacts/plan/phase-03.md))
- [ ] 3.1 Phase: 03; Scope: обновить CLI reference по новым endpoint-флагам, precedence и примерам использования; Output artifact: `docs/reference/cli-contract.md`, `README.md`; Verification: `make docs-check`; DoD: документация синхронизирована с фактическим CLI-контрактом и не содержит неоднозначностей.
- [ ] 3.2 Phase: 03; Scope: добавить unit/integration regression tests для include/exclude happy/error path и deterministic output; Output artifact: `tests/unit/**`, `tests/integration/**`; Verification: `uv run pytest tests/unit tests/integration -k "endpoint_filter"`; DoD: тесты фиксируют отсутствие регрессий при повторной генерации и для конфликтных фильтров.
- [ ] 3.3 Phase: 03; Scope: выполнить strict OpenSpec validation change-пакета и подтвердить implement readiness; Output artifact: `openspec validate 011-cmd-gen-additional-args --strict --no-interactive`; Verification: strict validation pass; DoD: пакет готов к отдельной implement-фазе.
